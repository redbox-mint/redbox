{
    "harvester": {
        "type": "workflow-harvester",
        "workflow-harvester": {
            "force-storage": "true"
        }
    },

    "transformer": {
        "harvestQueue": [],
        "indexOnHarvest": "false",
        "renderQueue": []
    },

    "indexer": {
        "script": {
            "type": "python",
            "rules": "name-authority.py"
        },
        "params": {
            "repository.name": "Newcastle",
            "repository.type": "Callista"
        }
    },

    "stages": [
        {
            "name": "pending",
            "label": "New Authority Record",
            "security": ["editor", "admin"],
            "visibility": ["guest"]
        },
        {
            "name": "live",
            "label": "Confirmed Authority",
            "security": ["editor", "admin"],
            "visibility": ["guest"],
            "template": "workflows/name-authority"
        }
    ]
}
